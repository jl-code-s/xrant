{
  "name": "xrant",
  "private": true,
  "scripts": {
    "dev": "npm run dev:server & npm run dev:client",
    "dev:server": "cd server && node index.js",
    "dev:client": "cd client && npx vite --host 0.0.0.0",
    "dev:install": "cd server && npm install && cd ../client && npm install"
  }
}
